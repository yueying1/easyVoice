# EasyVoice 项目对话交接文档

> 用途：把本文档内容复制给其他 AI 助手（如 DeepSeek），即可继续接手下面的工作。
> 生成时间：2026-09-22。原始完整对话记录（15MB，JSONL 格式）在：
> `C:\Users\13408\.zcode\cli\rollout\model-io-sess_d6547c7c-ce87-44d1-a280-6a7f67cea213.jsonl`

## 一、项目背景

- 项目：EasyVoice（github.com/yueying1/easyVoice，用户 fork 自 cosin2077/easyVoice）
- 功能：文本/小说转语音（Edge-TTS 免费合成 + DeepSeek 做 AI 推荐配音），Vue3 前端 + Express 后端 + ffmpeg 拼接，pnpm monorepo
- 本地路径：`F:\WenJian\XiaoShuo\Program\easyVoice`
- 用户环境：Windows 11 中文版，Node.js v24.15.0（装在 E:\Class\program\nodejs，全局目录无写权限），Git Bash

## 二、已完成的搭建工作

1. **克隆仓库**到上述路径
2. **pnpm**：无法全局安装（权限），解决方案是用 `corepack enable --install-directory <项目>\.bin pnpm` 把 pnpm 启动器放在项目内 `.bin\` 目录（版本 10.6.3，由 package.json 的 packageManager 字段固定）
3. **依赖**：`pnpm install -r` 完成（806 包），`.npmrc` 里设了 `store-dir=./.pnpm-store` 让缓存留在项目内
4. **ffmpeg**：系统没有，下载了 BtbN GitHub 的 win64-gpl 构建，解压放在项目内 `ffmpeg\bin\`（ffmpeg.exe/ffprobe.exe/ffplay.exe）
5. **构建**：前后端已 build，前端产物已复制到 `packages\backend\public`
6. **启动脚本**：项目根目录 `启动EasyVoice.bat`（纯 ASCII + CRLF，已实测）：
   - 设置 PATH 指向项目内 `.bin` 和 `ffmpeg\bin`
   - 启动前用 netstat 检测 3000 端口占用，被占则友好提示
   - 运行 `pnpm start`（生产模式，后端托管前端页面，端口 3000）
7. **DeepSeek 配置**：用户自己填好了项目根目录 `.env`（OPENAI_BASE_URL=https://api.deepseek.com，MODEL_NAME=deepseek-flash，Key 已配置且实测请求成功）。注意：官方没有 deepseek-flash 这个型号但请求成功了，如遇问题可改 deepseek-chat
8. 附加文件：`.env`（配置模板+实际值）、`使用说明.md`、`.gitignore` 已追加 `.pnpm-store/.bin/ffmpeg/.env` 等本地目录

## 三、已修复的三个问题（重要，改动在源码里）

1. **bat 中文乱码崩溃**：cmd 解析含中文/`chcp 65001` 的脚本会字节错位 → 改为纯英文提示的脚本
2. **端口占用（EADDRINUSE）**：是测试服务没关导致的，已在 bat 里加端口预检
3. **Edge-TTS WebSocket 断连导致服务进程崩溃（用户转小说时实际遇到，错误码 1006）**：
   - 根因：流式生成代码只监听流的 `end` 不监听 `error`，WS 断开触发 `destroy(err)`，未捕获的 error 事件使 Node 进程退出
   - 修复（已完成并实测）：
     - `packages/backend/src/services/tts.stream.service.ts`：新增 `pipeSegmentWithRetry()` 辅助函数（监听 error+end，失败自动重试 3 次、间隔 1 秒，耗尽则跳过该段继续），替换了 `generateWithLLMStream` 长文循环和 `buildSegmentList` 多段循环里裸的 pipe+on('end') 写法
     - `packages/backend/src/server.ts`：加了 `process.on('uncaughtException')` 和 `process.on('unhandledRejection')` 兜底，只记日志不退出
   - 已重新 build 并用 3 段文本实测通过（14.38 秒完整音频）

## 四、未完成的任务（接手后从这里继续）

> **状态：已于 2026-09-22 由后续 AI 接手完成，实现与验证明细见文末「六」节。**

**用户需求：MP3 输出到 `F:\WenJian\XiaoShuo\Program\easyVoice\audio`，SRT 字幕输出到 `F:\WenJian\XiaoShuo\Program\easyVoice\Str`**（注意用户写的是 "Str" 不是 "Srt"）

已做完的分析（改动点）：

- 目录常量在 `packages/backend/src/config/index.ts`：
  - `AUDIO_DIR = join(__dirname, '..', '..', 'audio')`（当前=packages/backend/audio，__dirname 是 dist）→ 改为项目根：`join(__dirname, '..', '..', '..', '..', 'audio')`
  - 需新增 `SRT_DIR` 指向项目根 `Str`，启动时 ensureDir
- 所有音频输出都用 `path.resolve(AUDIO_DIR, id)`，改 AUDIO_DIR 即自动生效；音频缓存 `.cache` 在 AUDIO_DIR 下，不受影响
- SRT 写入点分散且都是"音频路径.replace('.mp3','.srt')"派生的，涉及：
  - `tts.service.ts`：buildFinal 的 `generateSrt(finalJson, path.resolve(AUDIO_DIR, id.replace('.mp3','.srt')))`（约 150 行）、concatDirSrt 里 `outputFile.replace('.mp3','.srt')`（约 390 行）、buildSegment/buildSegmentList 返回的 `srt: ${STATIC_DOMAIN}/...` URL
  - `tts.stream.service.ts`：handleSrt（`audioPath.replace('.mp3','.srt')` 写文件与拼接）及各处返回 URL
  - 建议做法：加一个 `srtPathFor(audioPath)` 辅助函数统一把 AUDIO_DIR 替换成 SRT_DIR，替换所有派生点，避免遗漏
- 前端通过 `${STATIC_DOMAIN}/${id...}` 下载音频和字幕，静态托管在 `app.ts` → `middleware/static.ts`（`express.static(audioDir)` + `express.static(publicDir)`）→ 需加 `express.static(SRT_DIR)`；.mp3 与 .srt 扩展名不同、都挂在根路径不会冲突
- 改完需要：`pnpm --filter @easy-voice/backend build`（PATH 要含项目内 .bin），重启服务验证 mp3/srt 分别落在两个目录且网页能正常下载字幕

## 五、其他注意事项

- 用户上次崩溃前生成的残缺音频 `packages\backend\audio\aigen--第一卷----魔性不...mp3` 还在（改目录后旧文件不会自动迁移）
  - 补充（2026-09-22）：接手时 `packages\backend\audio` 已是空目录，没有旧文件需要迁移；项目根 `audio`、`Str` 当时也是空的（目录已被用户手动建好）
- 生成流程：文字 →（AI 推荐=DeepSeek 分段选音色）→ 微软 Edge-TTS 云端合成 → 本地 ffmpeg 拼接保存。需联网；不用 AI 推荐时文字只经过微软
- 项目还支持 REGISTER_KOKORO（本地开源 TTS 引擎），用户未来可能想搭
- 验证服务的测试方法：`curl -X POST http://localhost:3000/api/v1/tts/generateJson -H "Content-Type: application/json" -d '{"data":[{"text":"测试","voice":"zh-CN-XiaoxiaoNeural"}]}' -o out.mp3`（用项目内 `ffmpeg\bin\ffprobe.exe` 验证时长）

## 六、本次接手完成的工作（2026-09-22，MP3/SRT 分目录输出）

### 需求

MP3 → 项目根 `audio\`；SRT → 项目根 `Str\`（用户写的就是 Str）。

### 源码改动

| 文件 | 改动 |
|---|---|
| `packages/backend/src/config/index.ts` | 新增 `ROOT_DIR`（`__dirname` 向上 4 级，dist 与 tsx 开发两种运行方式都指向项目根）；`AUDIO_DIR = ROOT_DIR/audio`、新增 `SRT_DIR = ROOT_DIR/Str`；新增 `srtPathFor(audioPath)`：把音频目录内的路径映射成字幕目录内同名 `.srt`（不在音频目录内则退化为同级替换） |
| `packages/backend/src/services/tts.service.ts` | buildFinal、concatDirSrt 两处 `generateSrt` 的落盘路径改用 `srtPathFor()` |
| `packages/backend/src/services/tts.stream.service.ts` | `handleSrt`（非流式分支）、`concatDirSrt`、`buildFinal` 三处改用 `srtPathFor()`；新增 `streamingHeaders()` 统一流式响应头 |
| `packages/backend/src/services/edge-tts.service.ts` | `runEdgeTTS` 返回的 `srt` 改为 `srtPathFor(output)`（保持与真实文件一致）；`generateSrt` 写文件前 `mkdir -p` 目标目录，字幕目录不存在也不会失败 |
| `packages/backend/src/controllers/tts.controller.ts` | `/download/:file` 按扩展名选目录：`.srt` 取 `SRT_DIR`，其余取 `AUDIO_DIR`；`.srt` 的 Content-Type 改为 `application/x-subrip; charset=utf-8`（原来错误地用了 `audio/srt`） |
| `packages/backend/src/middleware/static.ts` / `app.ts` | 新增 `srtDir`，多注册一个 `express.static(SRT_DIR)`（`.mp3`/`.srt` 扩展名不同，挂同一层根路径不冲突） |
| `packages/backend/src/initApp.ts` / `server.ts` | `initApp()` 增加 `ensureDir(SRT_DIR)`；**并在 `app.listen` 回调里真正调用 `initApp()`**（原代码里 initApp 从未被调用，目录靠运气存在——首次运行时 `fs.writeFile` 会 ENOENT） |
| `packages/frontend/src/api/tts.ts` | 流式响应里读取新响应头 `x-generate-tts-srt`（服务端做了 URL 编码，因为 HTTP 头只能是 latin-1，文件名含中文），解码后挂到返回的 stream 对象上 |
| `packages/frontend/src/views/Generate.vue` | 长文本流式生成完成后把该字幕名写进音频列表项，使「下载字幕」按钮在长文本流程下也能出现（原先只有短文本/缓存命中才有字幕按钮） |
| `.gitignore` | 追加 `/Str/`（`**/audio` 已覆盖根 audio 目录） |
| `使用说明.md` | 更新「音频/字幕保存位置」及目录说明表 |

要点：所有音频落盘仍走 `path.resolve(AUDIO_DIR, id)`，改常量即全线生效；分段中间文件（`*.mp3_tmp\`、`*.srt.json`、`.cache\`）仍留在 `audio\` 下，不污染 `Str\`。

### 构建与验证（均已实测）

- `pnpm --filter @easy-voice/backend build` 通过；`pnpm --filter @easy-voice/frontend build` 通过（前端产物会自动 copy 到 `packages/backend/public`，已确认页面引用到新 bundle）
  - 注意：在本机的 DSH 沙箱里跑前端构建会因 esbuild 需要 piped stdio 而报 `spawn EPERM`，需放宽权限后执行；后端 tsc 构建不受影响。
- 接口实测 4 条路径，全部正确落盘且网页可下载：
  1. `/api/v1/tts/generate`（短文本直出）→ `audio\*.mp3` + `Str\*.srt`
  2. `/api/v1/tts/generateJson`（多段流式）→ 同上
  3. `/api/v1/tts/createStream` 普通长文本（325 字）→ mp3 822 KB / 68.54 s，srt 8 KB / 200 条
  4. `/api/v1/tts/createStream` **AI 推荐长文本（650 字，走 DeepSeek 分段 + 之前崩溃 1006 的那条分支）** → mp3 1.9 MB / 158.52 s，srt 16 KB / 404 条；服务进程全程未退出（第三节的兜底修复有效）
- 服务端保存的 mp3 与流式返回的字节/时长完全一致；`/api/v1/tts/download/<中文名>.srt` 与静态 `/<中文名>.srt` 均返回 200 与正确内容
- `packages\backend\audio` 保持为空，确认没有漏改的写入点
- 验证用的临时文件与测试音频已全部清理，`audio\`、`Str\` 现为空目录；测试服务已停止，端口 3000 已释放（用户下次双击 `启动EasyVoice.bat` 即可）

### 尚未做 / 需注意

- 未做真实浏览器点击验证（无浏览器自动化）：UI 改动只经接口 + 构建产物层面验证。用户实际使用时，长文本生成完成后应能看到「下载字幕」按钮。
- 若日后重新拉取代码或改了源码，需要重新执行前后端 build（前端 build 会自动同步到 `packages/backend/public`）。

## 七、整本小说按章节批量合成（2026-09-22 第二轮）

### 背景：需求与真实规模

用户希望"把整本小说 txt 拖进去 → 自动分章 → 每章一个 mp3，文件名带章序号"。

实测用户的书 `F:\WenJian\XiaoShuo\蛊真人错别字修改版4.0.txt`：
22,491,083 字节 / 7,789,253 字符 / 约 638.7 万汉字 / 43.8 万行 —— **用户原以为是"几十万字"，实际是 638 万字，差 100 倍**。

顺带弄清楚的两件事：

- **200 字不是模型限制**：`DIRECT_GEN_LIMIT`（`config/index.ts`）只是「同步接口 / 流式接口」的切换阈值，可在 `.env` 改；
  真正发往微软的单次文本是 `splitText()` 的 `TARGET_LENGTH = 500`。流式接口本身没有长度上限。
- 直接把 22MB 原文拖进原页面会连撞几道墙：浏览器 textarea + pinia `persist: true` 写 localStorage（上限约 5MB）、
  `express.json({ limit: '20mb' })` 返回 413、前端播放器把全部分片堆在内存（`utils/index.ts` 的 blobs 数组）。
  所以必须走「上传 → 分章 → 逐章串行合成」这条路。

### 章节切分：参考了本地 Reader 项目

用户提供了 `F:\WenJian\XiaoShuo\Program\Reader`（Win32 阅读器，fork 自 binbyu/Reader）源码与一份分析文档。
核心借鉴点来自 `Reader/TextBook.cpp`：

- `m_ValidChapter` 白名单：`第` 与单位字之间必须是数字/中文数字/大写数字/空白（我们额外加了 `〇`）；
- `ParserChaptersDefault` 的关键约束：**单位字（卷/章/部/节）后面必须紧跟空白、全角空格、NBSP、冒号或行尾**——
  这条能挡掉正文里「他翻到第三章」这类叙述句；
- 单位集合：卷/章/部/节（我们额外支持「回」）；
- 楔子/序章的特殊处理（我们用更多特殊篇章词：序/前言/引子/楔子/番外/后记/完本感言…）。

比 Reader 更严格的地方：**要求标题必须在行首**（Reader 是行内任意位置匹配，见 `TextBook.cpp:191`），
误判更少。另外卷在本项目里只作为分组信息（写进文件名），不单独生成音频。

实测（同一本书）：严格模式与宽松模式都是 **2336 章 / 7,293,201 字**，正文内残留标题行 **0** 处，
六卷边界（第一~第六卷）全部正确；作者请假占位章节（「卡文无更」「无更」，共 7 个 0 字章节）被自动跳过。
「序：不是走向成功，」这种被 epub 拆成两行的标题也做了续行合并。

### 源码改动

| 文件 | 改动 |
|---|---|
| `packages/backend/src/config/index.ts` | 新增 `BOOKS_DIR`（项目根 `books/`，保存上传原文便于续跑） |
| `packages/backend/src/services/chapter.service.ts` | **新增**：章节解析（白名单 + 分隔符 + 卷名 + 去重 + 续行合并 + 空章跳过）、`chapterFileName()`、小说文件保存/列表/缓存解析 |
| `packages/backend/src/services/chapterTask.service.ts` | **新增**：批量任务（串行逐章、断点续传、严格模式、整章重试、段进度回调、暂停/继续/取消） |
| `packages/backend/src/controllers/chapter.controller.ts` | **新增**：上传/列表/分页/启动/进度/暂停/继续/取消接口 |
| `packages/backend/src/routes/chapter.route.ts` | **新增**：`/api/v1/chapter/*`；上传用 `express.raw({limit:'100mb'})`，绕开全局 20mb JSON 限制 |
| `packages/backend/src/services/tts.service.ts` | 新增 `generateTTSWithId()`（自定义输出文件名）；`GenerateOptions` 支持 `useCache/cleanupTmp/strictSegments/segmentConcurrency/onSegmentProgress`；`concatDirAudio` 加超时兜底 |
| `packages/backend/src/services/edge-tts.service.ts` | 分段重试间隔由 200ms 提到 `EDGE_RETRY_BASE_DELAY_MS`（默认 1000ms），应对 1006 限流 |
| `packages/backend/src/routes/index.ts` | 挂载 chapter 路由 |
| `packages/backend/src/initApp.ts` | 启动时创建 `books/` |
| `packages/frontend/src/views/Chapter.vue` | **新增**：拖入 txt → 章节列表核对 → 范围/音色 → 开始 → 实时进度（含段进度与重试次数）→ 下载 |
| `packages/frontend/src/api/chapter.ts` | **新增**：章节相关接口封装 |
| `packages/frontend/src/utils/pendingBook.ts` | **新增**：跨页面传递刚拖入的文件 |
| `packages/frontend/src/views/Generate.vue` | 拖入 >1MB 的 txt 时自动转交章节页并带上文件；上传区加「按章节批量合成」入口 |
| `packages/frontend/src/router/index.ts` | 新增 `/chapter` 路由 |
| `.gitignore` | 追加 `/books/` |

### 关键设计

- **断点续传不依赖状态文件**：开始/继续时检查 `audio/<文件名>.mp3` 与 `Str/<文件名>.srt` 是否都已存在，
  存在即跳过。服务重启、断电后重新点「继续」即可，序号无关（文件名里带全书序号所以永远不冲突）。
- **严格模式**：批量任务里任一分段失败就整章判失败（不产出缺内容的音频），交给整章重试；
  非批量接口保持原有"部分成功也返回"的行为不变。
- **中间文件即时清理**：每章拼好后删除分段目录与字幕源 json。实测跑完 10 章后 `audio\` 里只有 10 个 mp3 + `.cache`，零残留。
- **ffmpeg 超时兜底**：沙箱环境下 fluent-ffmpeg 的 spawn 失败不会 reject promise（会变成 uncaughtException），
  曾导致任务永久卡死；现在 `concatDirAudio` 有 `CONCAT_TIMEOUT_MS`（默认 180s）兜底。

### 验证结果（全部实测）

- 前 10 章全部生成成功：跳过 3 章（断点续传生效）、成功 7 章、失败 0，用时 626 秒。
- 产物：10 个 mp3（61.9 MB / 90.1 分钟音频）+ 10 个 srt（1127~1680 条字幕），文件名形如
  `0002_第一卷_第一节：纵身亡魔心仍不悔.mp3`；单章 40~60 秒合成、7~10 分钟音频。
- 全书规模推算：2336 章 ≈ **351 小时音频 / 约 14.5 GB / 需连续跑 1.5~2 天**。
- 接口与页面：`/api/v1/chapter/books`、章节分页、`/chapter` 页面 bundle、中文名 srt 下载全部 200 通过。
- 已观察到的现象：微软免费接口会短时 1006 限流，靠"分段重试(1s 起) + 整章重试(8s 后)"扛过；
  页面上会显示「第 N 次尝试」避免用户误解进度回退。

### 尚未做

- 自定义章节正则：后端 `parseChapters(raw, { customRegex })` 已支持，但接口与页面还没暴露（等遇到特殊体例的书再加）。
- 未做真实浏览器点击验证（无浏览器自动化），页面只验证到接口与构建产物层面。
- 全书 2336 章未实际跑完（用户要求先验证前 10 章）。

## 八、启动脚本改进（2026-09-22 第三轮）

需求：启动后能直接看到要打开的网址（自动打开浏览器是加分项）。

最终做法（**只输出网址，不做自动打开**——用户确认自动打开如果不可靠就舍弃）：

1. `packages/backend/src/server.ts`：服务真正就绪后，控制台**最后一行**输出完整网址，
   倒数第二行输出章节合成页地址。全部 ASCII（Windows 控制台默认代码页下中文会乱码）：
   ```
   --------------------------------------------------
   Full novel (chapter by chapter): http://localhost:3000/chapter
   >>> Open this in your browser: http://localhost:3000 <<<
   ```
2. `启动EasyVoice.bat`：
   - 从 `.env` 读取 `PORT`（`findstr /b /c:"PORT="`），所以网址与实际端口一致；
   - 窗口标题带端口：`title EasyVoice Server  3000`，日志滚动后仍能看到；
   - 启动前打印一次网址（含 `/chapter`）；
   - **端口已被占用时不再报错退出**，而是提示"服务已在运行"并给出网址，等 5 秒后自动关窗；
   - 保持纯 ASCII + CRLF（用 `[System.IO.File]::WriteAllLines(..., ASCII)` 写入并校验过：
     0 个非 ASCII 字节、无裸 LF），避免之前踩过的 cmd 中文/代码页字节错位问题。

尝试过但放弃的方案：`start "" /b powershell ... TcpClient 探测端口 → Start-Process 打开浏览器`。
放弃原因：cmd 对 `cmd /c "..."` 内层 `|` 的转义规则难以确证（疑似 `^|` 被 PowerShell 当成字面量），
实测没看到浏览器被打开，而验证成本高（Edge 复用进程，无法靠进程数判断）。
按用户要求"打不开就舍弃，只输出网址"，最终选择最简单可靠的方案。

已实测：`启动EasyVoice.bat` 正常打印提示与网址、服务启动成功、最后一行即网址；
"端口已占用"分支也实测通过（提示 + 给出网址 + 自动关窗）。

## 九、按书名分文件夹存放（2026-09-22 第四轮）

需求：txt 生成的（整本小说章节合成）所有 mp3 放进一个以 txt 文件名命名的新文件夹。

最终结构（文件夹名 = 上传时 txt 的文件名去掉扩展名）：

```
audio\蛊真人错别字修改版4.0\0002_第一卷_第一节：纵身亡魔心仍不悔.mp3
Str\蛊真人错别字修改版4.0\0002_第一卷_第一节：纵身亡魔心仍不悔.srt
```

单篇文本生成（非章节模式）不受影响，仍直接放在 `audio\`、`Str\` 根目录。

### 改动

| 文件 | 改动 |
|---|---|
| `chapter.service.ts` | `ParsedBook` 新增 `folderName`；新增 `bookFolderNameFromId()`（bookId 形如「书名-内容hash8」，去掉尾部 hash 即文件夹名，重启后可复原） |
| `chapterTask.service.ts` | 任务新增 `folderName`；每章的 `outputId` 由 `文件名` 改成 `folderName/文件名`（`buildSegmentList` 用 `path.resolve(AUDIO_DIR, id)`，天然支持子目录）；断点续传判定、中间文件清理同步改用相对路径 |
| `tts.controller.ts` | 下载接口改走通配路由，接受 `书名/xxx.mp3` 相对路径；新增 `resolveInside()` 做目录穿越防护 |
| `tts.route.ts` | `router.get('/download/*', ...)`（保留旧的 `/download/:file` 兼容单篇） |
| `frontend/api/chapter.ts` | `ChapterTaskInfo` 新增 `folderName` |
| `frontend/api/tts.ts` | `downloadFile()` 改用 `encodeURI`（不用 encodeURIComponent，否则 `/` 会被编码成 `%2F`） |
| `frontend/views/Chapter.vue` | 下载时拼上 `folderName`；界面提示输出目录 |

### 关键点

- `srtPathFor()` 本来就用相对路径做映射，`audio/<书>/x.mp3 → Str/<书>/x.srt` **无需改动**，自动跟着走；
- `generateSrt()` 已有 `mkdir -p`，子目录会自动创建；
- 断点续传按新路径判定：**旧位置（根目录）的文件不会被认作已完成**，所以迁移或重跑要留意。

### 实测（2026-09-22）

- 前 2 章重新生成，产物正确落在 `audio\蛊真人错别字修改版4.0\` 与 `Str\蛊真人错别字修改版4.0\`，中间文件零残留。
- 下载接口带文件夹路径：mp3 7,573,004 bytes / srt 67,438 bytes，均 200；
  静态访问 `/<书>/<文件>.srt` 也 200。
- 目录穿越测试（`%2e%2e%2f%2e%2e%2f.env`）返回 400 拒绝。

### 背景说明

本轮开始时发现上一轮生成的 10 章产物与 `.cache` 已不在 `audio\`、`Str\` 里 ——
用户确认是**他自己删的**；`books\` 里多出的 `Demp-ef942c73.txt` 是他自己上传 Demp.txt 试用的记录
（说明他已经自己双击过启动脚本、用过网页端）。所以没有旧文件需要迁移，直接按新结构重新生成即可。

## 十、换电脑 / GitHub 部署文档（2026-09-22 第五轮）

需求：写一份「安装与运行前提 / 怎么才能跑起来」的文档，用于**推到 GitHub 后在另一台电脑拉取运行**。

### 交付：`安装与运行.md`

内容：运行原理（为什么需要 Node/联网/ffmpeg）、前提清单、新电脑上从零到启动的完整命令、
运行后自动生成的目录、macOS/Linux 差异、常见问题表、**上传 GitHub 前的密钥检查**、拉取新代码后的更新步骤。

### 顺带修掉的两个会让新电脑踩坑的问题

1. **`.env.example` 变量名写错**：原来只有两行，其中写的是 `OPENAI_KEY`，而代码读的是
   `OPENAI_API_KEY`（`config/index.ts`）——照抄模板的人会配了等于没配。
   已重写为完整模板：补上 `MODEL_NAME`、`PORT`、`LIMIT_TEXT_LENGTH`、`EDGE_API_LIMIT`、
   本次新增的 `EDGE_RETRY_BASE_DELAY_MS` / `CHAPTER_*` / `CONCAT_TIMEOUT_MS` 等，逐项加了中文注释。
2. **`启动EasyVoice.bat` 在纯净克隆上会直接失败**：它依赖项目内 `.bin\pnpm.cmd` 和已构建的 `dist`，
   而这两样都不在 Git 仓库里。已改为：
   - `.bin\pnpm.cmd` 不存在时自动 `corepack enable --install-directory .bin`；仍找不到 pnpm 就给出明确指引并退出；
   - 检测到 `packages\backend\dist\server.js` 不存在（首次运行）时，**自动执行 `pnpm install` + `pnpm build`**，失败时给出排查提示（`:buildfail`）；
   - 增加 ffmpeg 检测，缺失时警告（服务仍可启动，只是生成音频会失败）；
   - 保留原有的 `.env` 端口读取、端口占用分支、网址输出。
   仍是纯 ASCII + CRLF（写入后校验：0 非 ASCII、无裸 LF）。

### 上传 GitHub 的密钥安全检查（已执行）

- `git ls-files` 全量扫描 + 未跟踪的 md/bat/json 扫描：**未发现 `sk-` 开头的明文密钥**；
- `git check-ignore -v` 确认 `.env`、`.bin`、`ffmpeg`、`.pnpm-store`、`books`、`audio`、`Str` 全部被忽略；
- ⚠️ 提醒用户：`.npmrc`（本机为绕开全局写权限而设的 `store-dir=.pnpm-store`）**未被忽略**，
  新电脑不需要它，建议不要提交；另外新增的 7 个源文件是未跟踪状态，**必须 `git add -A`**，
  否则别人拉下来会编译失败（已在文档里用醒目提示写出）。

### 端到端验证（关键）

在工作区内复制出一份**只含源码**的纯净副本（排除 `node_modules`/`.env`/`ffmpeg`/`.bin`/产物），
按文档步骤完整跑了一遍：

| 步骤 | 结果 |
|---|---|
| `pnpm install` | ✅ 806 个包，5.7s（本机有 store 复用；新电脑是首次下载，会更久） |
| `pnpm build` | ✅ 三个包全部构建成功，`backend/dist/server.js`、`backend/public/index.html` 齐全 |
| 启动（`PORT=3100`） | ✅ 自动创建 `audio`/`Str`/`books`/`logs` 目录 |
| 页面 `/`、`/chapter`、`/generate` | ✅ 均 200 且返回 HTML |
| 接口 `/api/v1/chapter/books`、`/api/v1/tts/voiceList` | ✅ 200 |

验证后副本（335 MB）已删除，`git status` 无残留。

### 这一轮发现的两个知识点

1. **pnpm 10 默认忽略依赖包的构建脚本**：`pnpm install` 会打印
   `Ignored build scripts: esbuild, vue-demi`。**实测不影响本项目构建**（esbuild 走 optionalDependencies 拿二进制）。
   文档里已说明，并给出 `pnpm approve-builds` 作为出问题时的兜底。
2. **前端路由 `/chapter` 对不带 `Accept: text/html` 的请求返回 404**：这是 `connect-history-api-fallback`
   的正常行为（只重写浏览器导航请求）。用 curl / `Invoke-WebRequest` 裸测会误判成 bug，
   加 `Accept: text/html` 后 `/chapter`、`/generate`、`/about` 全部 200。
