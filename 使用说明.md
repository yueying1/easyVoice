# EasyVoice 本地运行说明

> 换电脑、或者从 GitHub 拉取代码后第一次运行，请看 **`安装与运行.md`**（装 Node.js / pnpm / ffmpeg、
> 构建、启动、常见报错的完整步骤）。本文只讲已经跑起来之后怎么用。

## 启动

双击 `启动EasyVoice.bat`。窗口会先显示要打开的网址；等服务真正就绪后，
**窗口最后一行**还会再显示一次：

```
>>> Open this in your browser: http://localhost:3000 <<<
```

把这一行的地址复制到浏览器打开就行（窗口标题栏也带着端口号，滚动日志时也能看到）。

- **整本小说按章节合成**：打开 http://localhost:3000/chapter
- **关闭那个黑色窗口 = 停止服务**
- 如果服务已经在运行，再双击脚本不会重复启动，而是直接告诉你服务已在运行并给出网址
- 如果你在 `.env` 里改过 `PORT`，脚本显示的网址会自动跟着变（它会读 `.env`）

## 整本小说：按章节批量合成（重点功能）

几十万字、上千章的整本小说，用这个页面：**http://localhost:3000/chapter**
（在「语音生成」页面拖入大文件时也会自动跳到这里）

用法：

1. 把整本小说的 txt 直接**拖进虚线框**（只支持 UTF-8 编码的 .txt，见下面的编码说明）；
2. 页面自动识别章节，显示「共多少章 / 多少万字」，并列出章节核对是否切分正确；
3. 选音色、选章节范围（有「前 10 章 / 前 20 章 / 全部」快捷键），点「开始生成」；
4. 每章生成一个 mp3 + 一个字幕，并且**按书名单独建一个文件夹**
   （文件夹名就是 txt 的文件名去掉 `.txt`）：

   ```
   audio\蛊真人错别字修改版4.0\0002_第一卷_第一节：纵身亡魔心仍不悔.mp3
   Str\蛊真人错别字修改版4.0\0002_第一卷_第一节：纵身亡魔心仍不悔.srt
   ```

   文件名前缀是全书连续序号（4 位补零），所以按文件名排序就是阅读顺序。
   换成别的 txt 只会新建别的文件夹，不会和这本书的文件混在一起。

**可以随时中断**：关掉窗口/断电都行，重新点「继续」或重新开始，已经生成好的章节会自动跳过，
只补没做完的。音频在 `audio\<书名>\`，字幕在 `Str\<书名>\`。

关于耗时（以 600 万字、2336 章的书实测）：单章约 3000 字，合成需要 40~60 秒、产出 7~10 分钟音频；
全书跑完约需 **1.5~2 天不间断运行**，总体积约 **14 GB**。建议先试「前 10 章」确认音色和效果再排全书。

章节识别规则（参考了阅读器软件的成熟做法）：行首 `第X章/节/部/回` + `第X卷` 卷标题，
并且要求「章」字后面紧跟空格或冒号（这样正文里"他翻到第三章"这类句子不会被误认成章节），
也支持「序、楔子、番外、后记」等独立篇章。如果某本书的目录格式很特殊（如 `1. 标题`）识别不出来，
告诉我，可以加自定义正则规则。

**txt 必须是 UTF-8 编码**：用记事本打开另存为时选「UTF-8」，不要选「ANSI」（ANSI 是 GBK，读进来会乱码，
页面会直接提示编码不对）。

## 生成的音频和字幕保存在哪里

**整本小说（按章节批量合成）**：按书名单独建文件夹，文件夹名 = txt 文件名去掉扩展名

```
audio\蛊真人错别字修改版4.0\0002_第一卷_第一节：纵身亡魔心仍不悔.mp3
Str\蛊真人错别字修改版4.0\0002_第一卷_第一节：纵身亡魔心仍不悔.srt
```

**单篇文本生成**（不涉及章节的普通用法）：直接放在 `audio\`、`Str\` 根目录下。

其他：

- **上传的小说原文**：项目根目录 `books\`（保留原文，用于万一中断后继续跑）
- 这些目录会在服务启动时自动创建，网页上点「下载音频 / 下载字幕」也是从这些目录取文件
- 分段合成的中间文件会在每章完成后自动清理，`audio\` 里最终只会留下成品 mp3（以及 `.cache\` 缓存目录）

## AI 智能推荐配音（可选）

不配置也能正常用 Edge-TTS 免费配音，只是网页上的"AI 推荐"按钮用不了。

需要时打开本目录下的 `.env` 文件，取消注释并填写：

```
OPENAI_BASE_URL=https://api.openai.com/v1     （或其他 OpenAI 兼容地址，如 DeepSeek）
OPENAI_API_KEY=sk-xxxx
MODEL_NAME=模型名
```

改完 `.env` 后重启服务（关窗口再开）。

## 常见问题

- **端口被占用**：编辑 `.env`，加一行 `PORT=3001`（数字随意）。
- **想加快长文合成速度**：`.env` 里加 `EDGE_API_LIMIT=5`（1~10，别太大）。
- **换了新电脑/删了 node_modules**：装好 Node.js 后，在项目目录运行 `corepack pnpm install -r`，再双击启动脚本即可（pnpm 已内置在 `.bin` 目录）。
- **章节没识别出来 / 识别多了**：先看页面的章节列表。识别不出来多半是目录体例特殊（如 `1. 标题`），
  或是 txt 不是 UTF-8。
- **批量合成中频繁重试、变慢**：微软免费接口会短时限流（日志里是 WebSocket 1006 断连）。
  默认已经做了两层重试（分段重试 + 整章重试），一般能自动扛过去。
  想更稳可以调 `.env`：

  ```
  CHAPTER_SEGMENT_CONCURRENCY=1    # 一章内同时合成的段数（默认 2，调 1 最稳最慢）
  CHAPTER_MAX_ATTEMPTS=3           # 整章失败后重试次数（默认 2）
  CHAPTER_RETRY_DELAY_MS=15000     # 整章重试前等待毫秒（默认 8000）
  EDGE_RETRY_BASE_DELAY_MS=2000    # 分段重试间隔基数毫秒（默认 1000）
  ```
- **跑了一半想停**：页面点「暂停」或直接关掉窗口都行，下次点「继续」会跳过已生成的章节。

## 目录说明（本次安装新增的部分）

| 目录/文件 | 作用 |
|---|---|
| `.bin\` | pnpm 启动器（电脑没装全局 pnpm 也能运行） |
| `ffmpeg\` | 音频拼接用的 ffmpeg（后端合成语音必需） |
| `.pnpm-store\` | 依赖包本地缓存 |
| `启动EasyVoice.bat` | 一键启动脚本 |
| `.env` | 环境变量配置（API 密钥等） |
| `audio\` | 生成结果：音频 mp3 |
| `Str\` | 生成结果：字幕 srt |
| `books\` | 上传的整本小说原文（便于中断后续跑） |

以上内容均已加入 `.gitignore`，不会被提交到你 fork 的 GitHub 仓库。
