# 安装与运行（换电脑 / 从 GitHub 拉取后怎么跑起来）

> 适用场景：把本项目推到 GitHub，然后在**另一台电脑**上拉取并运行。
> 本文以 **Windows** 为例（本项目主要在 Windows 上验证）。macOS / Linux 的差异在第五节说明。

---

## 一、它是怎么跑起来的（先理解前提）

```
浏览器页面（Vue 3）
      │  输入文字 / 拖入小说 txt
      ▼
后端 Node.js（Express，端口 3000）
      │  按标点分段
      ▼
微软 Edge-TTS 云端接口（免费，必须联网）
      │  每段返回 mp3
      ▼
本地 ffmpeg 把分段拼成一个 mp3（+ 生成 srt 字幕）
      ▼
保存到  audio\  和  Str\   目录
```

由此得出三个硬性前提：**要有 Node.js 跑后端**、**要联网**（合成在微软云端做）、**要有 ffmpeg**（拼接音频）。

「AI 推荐配音」是可选功能，它额外调用一个 OpenAI 兼容接口来给不同角色分配音色；不配也能用，只是这个按钮用不了。

---

## 二、运行前提

| 依赖 | 版本 / 说明 | 必需 |
|---|---|---|
| **Node.js** | 20 LTS 及以上（本机实测 v24.9.0 可用） | ✅ 必需 |
| **pnpm** | 10.6.3（`package.json` 里已固定版本，Node 自带的 corepack 可自动获取） | ✅ 必需 |
| **ffmpeg** | 近期任意版本，需同时包含 `ffmpeg.exe` 和 `ffprobe.exe` | ✅ 必需 |
| **网络** | 合成走微软 Edge-TTS 云端接口 | ✅ 必需 |
| OpenAI 兼容 API | DeepSeek / OpenAI / OpenRouter… 仅「AI 推荐」需要 | ⬜ 可选 |
| 磁盘空间 | 整本小说合成会产生大量音频（600 万字的小说约 15 GB） | ⬜ 视用途 |

---

## 三、新电脑上的完整步骤

下面命令在 **PowerShell** 里逐段执行即可。

### 1. 安装 Node.js

```powershell
winget install OpenJS.NodeJS.LTS
```

装完**关掉并重新打开** PowerShell，确认：

```powershell
node -v      # 应输出 v20.x 或更高
```

> 没有 winget 就去 https://nodejs.org 下载 LTS 版安装包。

### 2. 准备 pnpm

Node.js 自带 corepack，**不需要全局安装 pnpm**，两种方式任选：

```powershell
# 方式 A：直接让 corepack 代跑（不修改任何全局设置，最省事）
corepack pnpm -v

# 方式 B：启用 pnpm 命令（如果 Node 安装目录可写）
corepack enable
pnpm -v
```

两种都行。后面命令里凡是写 `pnpm xxx` 的地方，如果你用的是方式 A，就改成 `corepack pnpm xxx`。

> 如果 `corepack enable` 报权限错误（Node 装在 `C:\Program Files` 之类），
> 用方式 A，或者把启动器装到项目里：`corepack enable --install-directory .bin`

### 3. 拉取代码

```powershell
git clone https://github.com/yueying1/easyVoice.git
cd easyVoice
```

> 仓库地址换成你自己的。国内网络拉 GitHub 慢的话，可以用镜像或在网页上下 Zip 解压。

### 4. 安装依赖

```powershell
pnpm install
```

第一次会下载几百个包，**几分钟**属正常。完成后目录里会出现 `node_modules`。

### 5. 准备 ffmpeg

**方式 A：装到系统里（推荐，最简单）**

```powershell
winget install Gyan.FFmpeg
```

装完**重新打开** PowerShell，确认：

```powershell
ffmpeg -version
```

**方式 B：放进项目目录（和启动脚本配套，不用改系统 PATH）**

1. 下载 Windows 构建：
   - https://www.gyan.dev/ffmpeg/builds/ → `ffmpeg-release-essentials.zip`
   - 或 https://github.com/BtbN/FFmpeg-Builds/releases → `ffmpeg-master-latest-win64-gpl.zip`
2. 解压后进入 `bin` 子目录，把 `ffmpeg.exe`、`ffprobe.exe`、`ffplay.exe` 三个文件
   复制到项目的 `ffmpeg\bin\` 目录下（该目录不在 Git 仓库里，需要自己建）：

```
easyVoice\ffmpeg\bin\ffmpeg.exe
easyVoice\ffmpeg\bin\ffprobe.exe
```

`启动EasyVoice.bat` 会自动把 `ffmpeg\bin` 加进 PATH，所以放这里就能被找到。

> ffmpeg 缺失时服务仍能启动，但**一生成音频就会失败**，启动脚本也会给出警告。

### 6. 配置 .env（可选，但建议）

```powershell
copy .env.example .env
notepad .env
```

- 不改也能跑（Edge-TTS 免费合成可用，只是「AI 推荐」用不了）
- 想用「AI 推荐」就填 `OPENAI_BASE_URL` / `OPENAI_API_KEY` / `MODEL_NAME`
  （DeepSeek 就填 `https://api.deepseek.com`、你的 key、`deepseek-chat`）
- 端口被占用时，取消 `# PORT=3000` 的注释并改成别的端口

完整可配置项（含各项调优参数）见 `.env.example` 里的注释。

### 7. 构建

```powershell
pnpm build
```

这一步做两件事：把后端 TypeScript 编译到 `packages/backend/dist`，
把前端 Vue 打包并**自动复制**到 `packages/backend/public`。

> 以后再 `git pull` 拉到新代码，**要重新执行一次 `pnpm build`**（改了依赖就先 `pnpm install`）。

### 8. 启动

```powershell
pnpm start
```

看到下面这样的输出就成功了，照最后一行提示打开浏览器：

```
Server running on port 3000
--------------------------------------------------
Full novel (chapter by chapter): http://localhost:3000/chapter
>>> Open this in your browser: http://localhost:3000 <<<
```

**Windows 用户更省事的做法**：装好依赖和 ffmpeg 之后，直接双击 `启动EasyVoice.bat`。
它会自动：创建 pnpm 启动器（如果缺）→ 首次运行时自动装依赖并构建 → 检查 ffmpeg
→ 检查端口占用 → 打印要打开的网址。关掉窗口 = 停止服务。

> **本节流程已实测**：在一份只含源码的纯净副本上（没有 `.env`、没有 `node_modules`、没有 ffmpeg）
> 完整跑了一遍 `pnpm install` → `pnpm build` → 启动 → 访问页面与接口，全部通过。
> 其中 `pnpm install` 会打印一条 `Ignored build scripts: esbuild, vue-demi` 警告，
> 属于 pnpm 10 的默认行为，**实测不影响后续构建**，可以忽略（详见第六节）。

---

## 四、运行后会生成哪些目录

Git 仓库里**只有源码**，下面这些目录都是运行/构建时自动产生的（已被 `.gitignore` 排除）：

| 目录 | 内容 | 谁生成 |
|---|---|---|
| `node_modules\` | 依赖包 | `pnpm install` |
| `packages\backend\dist\` | 后端编译产物 | `pnpm build` |
| `packages\backend\public\` | 前端打包产物（由后端托管） | `pnpm build` |
| `.bin\` | 项目内的 pnpm 启动器 | `启动EasyVoice.bat` / corepack |
| `ffmpeg\bin\` | 你手动放入的 ffmpeg | 手动 |
| `audio\<书名>\` | **成品 mp3** | 生成时 |
| `Str\<书名>\` | **字幕 srt** | 生成时 |
| `books\` | 上传的小说原文（便于中断后续跑） | 上传 txt 时 |
| `logs\` | 运行日志 | 运行时 |

说明：

- 单篇文本生成的产物直接放在 `audio\`、`Str\` 根目录；**整本小说**按章节合成的产物会
  归到以 txt 文件名命名的子文件夹，例如 `Demo.txt` → `audio\Demo\0001_….mp3`
- 分段合成的中间文件在每章完成后自动清理

---

## 五、日常怎么用

启动后浏览器打开 **http://localhost:3000**：

- `/` 首页
- `/generate` 单篇文本生成（粘贴文字，一段一个 mp3）
- `/chapter` **整本小说按章节批量合成**（把 txt 拖进去，自动分章，一章一个 mp3）

### macOS / Linux 用户

代码本身跨平台，区别只在启动方式：

```bash
corepack pnpm install
corepack pnpm build
corepack pnpm start
```

ffmpeg 用 `brew install ffmpeg`（macOS）或 `sudo apt install ffmpeg`（Debian/Ubuntu）。
`启动EasyVoice.bat` 是 Windows 专用脚本，其他系统请用上面的命令。

---

## 六、常见问题

| 现象 | 原因与处理 |
|---|---|
| `pnpm : 无法将“pnpm”项识别为…` | 没启用 pnpm。用 `corepack pnpm xxx`，或 `corepack enable` 后重开终端 |
| `corepack enable` 提示 EPERM / 拒绝访问 | Node 安装目录没写权限。改用 `corepack pnpm xxx`，或 `corepack enable --install-directory .bin` |
| 生成音频时报 ffmpeg 相关错误 | ffmpeg 没装或不在 PATH。按第三节第 5 步处理；放 `ffmpeg\bin\` 时要用启动脚本启动 |
| 启动时提示端口被占用 | 已有服务在跑（关掉旧窗口），或改 `.env` 里的 `PORT` |
| `pnpm install` 卡住 / 很慢 | 网络问题。国内可设置镜像：`pnpm config set registry https://registry.npmmirror.com` |
| 安装时提示 `Ignored build scripts: esbuild, vue-demi` | pnpm 10 默认不执行依赖包的安装脚本。**实测不影响本项目构建**；万一构建时报 esbuild 相关错误，执行 `pnpm approve-builds` 允许这两项再重试 |
| 浏览器打开 `/chapter` 之类地址显示 404 | 先确认用的是浏览器访问（而不是 curl 之类不带 `Accept: text/html` 的工具）；再确认执行过 `pnpm build` |
| 安装依赖时出现 node-gyp / 编译错误 | 极少数平台缺预编译包。装一下 VS Build Tools（勾选「使用 C++ 的桌面开发」）后重试 |
| 长文本合成中途报 1006 断连 | 微软免费接口短时限流，程序会自动重试。想更稳：`.env` 里设 `CHAPTER_SEGMENT_CONCURRENCY=1`、调大 `CHAPTER_RETRY_DELAY_MS` |
| 拖入的 txt 识别不出章节 | txt 必须是 **UTF-8** 编码（记事本另存为时选 UTF-8，别选 ANSI/GBK）；识别规则见 `使用说明.md` |
| 页面打开是空白 / 404 | 没执行 `pnpm build`，后端没有前端产物可托管 |
| 改了源码但页面没变化 | 需要重新 `pnpm build`，然后重启服务（前端产物由后端托管） |

---

## 七、把项目上传到 GitHub（含密钥安全检查）

### 上传前必须确认：别把密钥传上去

`.gitignore` 里已经排除了 `.env`、`books/`、`audio/`、`Str/`、`ffmpeg/`、`.pnpm-store/`、
`.bin/`、`node_modules/`、`**/dist/**`。上传前建议实际验证一遍：

```powershell
git status --short                 # 看看将要提交什么
git check-ignore -v .env           # 应输出 .gitignore:N:.env，说明被忽略
git ls-files | Select-String "\.env$"   # 不应有输出（.env.example 除外）
```

**千万不要**用 `git add -f .env`（`-f` 会强行突破忽略规则）。

### 提交与推送

```powershell
git add -A            # 注意：必须用 -A 或 . ，把新增的源码文件一起加上
git commit -m "feat: 整本小说按章节批量合成、产物按书名分文件夹"
git push origin main
```

> ⚠️ **常见坑**：本项目新增了一批源文件（`chapter.service.ts`、`chapterTask.service.ts`、
> `chapter.controller.ts`、`chapter.route.ts`、`Chapter.vue`、`api/chapter.ts`、
> `pendingBook.ts` 等）。如果只 `git add` 了被修改的文件而漏掉这些**新文件**，
> 别人拉下来会**编译失败**。用 `git add -A` 最稳妥，或者 add 之后用
> `git status` 再确认一次没有漏掉的 `??` 文件。

### 上传后仓库里会有什么

- 源码：`packages/{backend,frontend,shared}/src`、`scripts/`、配置文件
- 文档：`README.md`、`使用说明.md`、`安装与运行.md`（本文）、`对话交接文档.md`
- 启动脚本：`启动EasyVoice.bat`
- 依赖锁：`pnpm-lock.yaml`、`pnpm-workspace.yaml`、`package.json`
- 环境变量模板：`.env.example`

**不会**包含：小说原文、音频产物、ffmpeg、依赖包、`.env`（含密钥）。

---

## 八、在新电脑上更新代码

```powershell
git pull
pnpm install     # 只有依赖有变化时才需要，但执行一次没坏处
pnpm build       # 源码变了就必须重新构建
```

然后重新启动服务（`pnpm start` 或双击 `启动EasyVoice.bat`）。

> `启动EasyVoice.bat` 只会在 `packages\backend\dist\server.js` **不存在**时自动构建；
> 拉取新代码属于「已构建过」，所以要手动 `pnpm build` 一次。
